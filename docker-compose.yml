services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./temp_files:/app/temp_files
      - ./outputs:/app/outputs
    deploy:
      resources:
        limits:
          memory: 4G